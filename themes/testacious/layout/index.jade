mixin link(post)
	- var attrs = {}
	if post.img
		- attrs.style = { 'background-image': 'url(' + url_for(post.img) + ')' }
	a.posts-item(href=url_for(post.path))&attributes(attrs)
		h2 #{post.title}

nav.posts-list
	each post in page.posts.toArray()
		+link(post)
